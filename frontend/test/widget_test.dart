import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Teste básico será adicionado depois
    expect(1 + 1, equals(2));
  });
}
